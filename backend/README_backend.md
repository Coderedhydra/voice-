# Backend Setup

1. Create a Python virtual environment:
python3 -m venv venv
source venv/bin/activate

markdown
Copy code

2. Install dependencies:
pip install -r requirements.txt

markdown
Copy code

3. Run backend:
python app.py

pgsql
Copy code

4. Update `frontend/js/main.js` BACKEND_URL to point to your backend URL.